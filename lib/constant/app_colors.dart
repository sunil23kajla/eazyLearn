import 'package:flutter/material.dart';

class AppColors {
  static const Color or = Color(0xFF1E1E1E);
}

class Sizess {
  height(context) => MediaQuery.of(context).size.height;
  width(context) => MediaQuery.of(context).size.width;
}
